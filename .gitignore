# data/pages/ NÃO é mais ignorada: na nuvem as imagens dos encartes precisam
# ficar no repositório (é o que preserva a visão do painel entre execuções).
data/*.log
data/.ultima_execucao
data/.lock/
painel-encartes.html
.DS_Store
data/.pub_stamp
data/.tentativas
# backups pontuais criados pelos scripts — o histórico do git já é o backup
data/*.bak-*
# site de PRÉVIA (link de teste): id/url ficam só na máquina que publicou
data/netlify_site_id_teste
data/netlify_url_teste
tools/node_modules/

# artefatos do Python
__pycache__/
*.pyc
